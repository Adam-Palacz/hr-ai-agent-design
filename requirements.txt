# Core dependencies
pydantic>=2.0.0
pydantic-settings>=2.0.0
langchain>=0.1.0
langchain-core>=0.1.0
langchain-openai>=0.1.0
openai>=1.0.0

# PDF processing
PyPDF2>=3.0.0

# PDF to image conversion for OCR (install at least one)
# Option 1: pdf2image (requires poppler)
pdf2image>=1.16.0
# Option 2: PyMuPDF (alternative, no external dependencies)
pymupdf>=1.23.0

# Image processing
Pillow>=10.0.0

# Environment variables
python-dotenv>=1.0.0

# Configuration file parsing
pyyaml>=6.0.0

# Optional: For better PDF parsing
# pdfplumber>=0.10.0
# pymupdf>=1.23.0

# Web application
Flask>=3.0.0
Werkzeug>=3.0.0

